{
  "name": "Node.js",
  "settings": { 
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "dbaeumer.vscode-eslint"
  ],
  "postCreateCommand": "source .env && npm install && npm run start",
  "forwardPorts": [3000]
}